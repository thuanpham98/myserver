<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title  #{title}
        //---add icon---///
        link(rel="icon" , type="image/png", href="images/icon.png")
        //--add a css file--//
        link(rel="stylesheet" href="/stylesheets/style.css")
        //--add js file---//

    body
        div
            h1(class="center") Access
            form(action='/access', method='post')
                input(type="text" name="name" placeholder="Your name..")
                input(type="text" name="pass" placeholder="Your password..")
                input(type="text" name="id" placeholder="Your ID..")
                input(type='submit', value='Log in')
        br
    form(action='/asign', method='get')
        h2(class="center") if you don't have acount , click here
        input(type='submit', value='Asign')

    form(action='/', method='get')
        br
        hr
        input(type='submit', value='Home',style="width: 10%;background-color: red")